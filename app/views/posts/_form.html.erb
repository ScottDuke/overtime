<%= form_with model: @post do |f| %>
  <div class="form-group">
    <%= f.date_field :date %>
     <div id="date_errors">
  </div>
  <div class="form-group">
    <%= f.text_area :rationale %>
  </div>
  <%= f.submit("Save") %>
<% end %>