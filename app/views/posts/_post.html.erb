 <tr>
  <td>
    <%= post.id %>
  </td>
  <td>
    <%= post.date %>
  </td>
  <td>
    <%= post.overtime_request %>
  </td>
  <td>
    <%= post.user.full_name %>
  </td>
  <td>
    <%= link_to(truncate(post.rationale), post) %>
  </td>
   <td>
    <span class="">
      <%= state_badge(post.state) %>
    </span>
  </td>
  <td>
    <%= link_to("<i class='fas fa-edit'></i>".html_safe, edit_post_path(post), { id: "edit_#{post.id}", class: "index-icons" }) if policy(post).update? %>
  </td>
  <td>
    <%= link_to("<i class='fas fa-trash-alt'></i>".html_safe, post_path(post), { method: :delete, id: "delete_#{post.id}", class: "index-icons", data: { confirm: "Are you sure?" } }) %>
  </td>
</tr>      